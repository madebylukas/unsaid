{
  "name": "shh-type",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A webcam-only, personalized silent-word demo with visible ambiguity decoding.",
  "scripts": {
    "postinstall": "node scripts/setup-assets.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.22-rc.20250304",
    "@tensorflow/tfjs": "^4.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
